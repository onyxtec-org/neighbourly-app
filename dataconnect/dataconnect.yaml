specVersion: "v1"
serviceId: "neighbourly"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "neighbourly-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
